export default function GridMenu() {
  return (
    <div  className="text-darkBlue">
      depends on what list you want to see, this page will display either
      grid-menu of cities(clickable) or a grid-menu of tours also clickable with
      links
    </div>
  );
};

// export default GridMenu;